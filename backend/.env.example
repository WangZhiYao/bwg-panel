# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略）
SECRET_KEY=            # 留空：首次启动自动生成并写回
ADMIN_PASSWORD=        # 首次启动的管理员初始密码；留空则自动生成并打印到控制台
DB_PATH=               # SQLite 路径，默认 backend/data/panel.db
COOKIE_SECURE=0         # 反代 HTTPS 部署时改 1：登录 Cookie 仅经 HTTPS 传输
